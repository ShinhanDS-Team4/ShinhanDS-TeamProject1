<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.saren.prod">

	<select id="selectByProdId" parameterType="int"
		resultType="prodVO">
		SELECT *
		FROM PROD
		WHERE "prod_id" = #{prod_id}
	</select>

	<select id="selectByMemberId" parameterType="string"
		resultType="prodVO">
		SELECT *
		FROM PROD
		WHERE "member_id" = #{member_id}
	</select>

	<select id="selectAll" resultType="prodVO">
		SELECT *
		FROM PROD
	</select>

	<select id="selectByCategory" parameterType="int" resultType="map">
		SELECT
			M.brand,
			P.prod_name,
			P.prod_price,
			PI.img_url
		FROM
			PROD P
		JOIN
			MEMBER M ON P.member_id = M.member_id
		LEFT JOIN
			PROD_IMAGE PI ON P.prod_id = PI.prod_id
		WHERE
			P.category_id = #{aa}

	</select>

	<insert id="prodInsert" parameterType="prodVO">
		INSERT
		INTO PROD("prod_id",
		"prod_name", "prod_desc", "prod_price",
		"prod_added_date",
		"category_id", "member_id")
		VALUES(#{prod_id}, #{prod_name},
		#{prod_desc}, #{prod_price},
		#{prod_added_date}, #{category_id},
		#{member_id})
	</insert>

	<update id="prodUpdate" parameterType="prodVO">
		UPDATE PROD
		SET
		"prod_name"
		= #{prod_name}, "prod_desc" = #{prod_desc},
		"prod_price" =
		#{prod_price}, "prod_added_date" = #{prod_added_date},
		"category_id" =
		#{category_id}, "member_id" = #{member_id}
		WHERE
		"prod_id" = #{prod_id}
	</update>

	<delete id="prodDelete" parameterType="int">
		DELETE
		FROM PROD
		WHERE
		"prod_id" = #{prod_id}
	</delete>

	<select id="selectAll2" resultType="map">
		SELECT
		M.brand,
		P.prod_name,
		P.prod_price
		FROM
		MEMBER M
		JOIN
		PROD P ON M.member_id = P.member_id
	</select>

</mapper>
